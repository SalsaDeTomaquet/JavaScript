//  Escribe una función llamada getFirstElement que reciba un array y devuelva el primer elemento.

// Por ejemplo:


let elemento = getFirstElement = (arr) => {

    return arr[0];
}
let c = getFirstElement([1, 2]);
console.log(c)
