// Recorrer la siguiente lista con un bucle imprimiendo el doble de cada número:
let numbers = [1, 9, 3, 8, 5, 7];
for (let i = 0; i < numbers.length; i++) {
    let resultado = numbers[i] * 2
    console.log(resultado)
}