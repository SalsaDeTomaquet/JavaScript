function booleano(error) {
    if (error == true) {
        console.log(" «Sí, eso truees cierto»")

    } else {
        console.log("«No, eso es falso»") //Porque 'error'y string '1' no tienen el mismo valor
    }


}
booleano(true)