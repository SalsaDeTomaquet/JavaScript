// Crear y llamar a una función que recibe la base y la altura de un triángulo y calcula su área.Crear y llamar a una función que recibe la base y la altura de un triángulo y calcula su área.
function calcularArea(alto, ancho) {
    area = alto * ancho / 2;
    console.log("El area del triangulo es: " + area)

}
calcularArea(3, 4);